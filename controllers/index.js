// exporting file
// this will export the auth controller or/ any other controller file created
module.exports = {
    auth: require('./auth'), 
    user: require('./user'),
    design: require('./design')
}